// For each chunk in currentBlockchainEntries , the worker attempts to write to the blockchain.
const getBlockchainEntries = async () => {
  console.log("Getting blockchain entries");
};

// Send the data to the blockchain.
const sendToBlockchain = async () => {
  console.log("Sending to blockchain");
};

// The worker records the txnId and other relevant details into blockchainTransactions table.
const blockchainTransactions = async () => {
  console.log("Inserting blockchain transactions");
};